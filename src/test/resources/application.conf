# This is the config file for the krispii-api application.
application.langs="en"
app.cache.expires = 5

db.postgresql.username="test_user"
db.postgresql.password="test_user"
db.postgresql.host="localhost"
db.postgresql.database="testdb"
db.postgresql.port=5433

# Time between new revisions in seconds.
response.newRevision.timeout = 180

